import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rsapp.rustbook',
  appName: 'Rust Book App',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
